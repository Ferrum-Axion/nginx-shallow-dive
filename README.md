# Nginx Shallow Dive
# Nginx – Project Axion

This repository contains an example nginx virtual host configuration.

## Features
- Virtual host named `project_axion`
- Root directory under `/srv`
- Autoindex enabled
- Tested with `nginx -t` and `curl`

